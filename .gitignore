node_modules/
.env*
!.env.example
*.log
.DS_Store

# локальное файловое хранилище пользователей — содержит их данные, не должно быть в репо
src/storage/db.json

# защита от случайного попадания в репо приватных ключей (SSH и т.п.),
# даже если кто-то по ошибке положит их внутрь папки проекта
*.key
*.key.pub
*.pem
id_rsa*
